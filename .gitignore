# Dependencies
node_modules/

# Wrangler / Cloudflare
.wrangler/
dist/

# Generated build artifacts (Digital Twin SQL, etc.)
tmp/
# Local source datasets pulled for offline twin/grid builds (ERA5, etc.)
data/
# prototype/data/aragon-density.geojson is generated but committed — it is the
# static dataset the Cloudflare Pages map serves (regenerate: node prototype/build-geojson.mjs).

# Secrets (never commit)
.dev.vars
.env

# OS
.DS_Store
